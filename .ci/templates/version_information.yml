---

steps:
  - script: |
      cat /etc/lsb-release
      parallel --version
      yamllint --version
      python --version
      ansible --version
      vagrant version
      exit 0
    displayName: Gather Build Environment Information
